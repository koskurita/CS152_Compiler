function main;
beginparams
x : integer;
a, b : integer;
c : array [21] of integer;
d, e : array [23] of integer;
endparams

beginlocals

n : integer;

endlocals

beginbody	## main program
		n[1] := 3;
		n := n[1];
		x := 5;
		
		do
			beginloop
				c[a] := a;
				a := a + 1;
				read x;
				read a, e[1], b;
			endloop
		while a < x;
		
                b := 0;

		while b < x
			beginloop
				b := b + 1;
		endloop;

endbody
